<!--This is a partial to identify error in login and registration forms-->
<% if(typeof errors != 'undefined'){ %> <% errors.forEach(function(error) { %>
    <%= error.msg %>
    <p></p>
  <% }); %>
<% } %>

<% if(typeof xpage_message != 'undefined'){ %>
  <%= xpage_message %>
  <p></p>
<% } %>